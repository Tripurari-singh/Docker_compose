version: "3.8"

services:
  app:
       build: .
       depends_on: 
         - postgres_db
       ports:
         - 3000:3000


  postgres_db:
       image: postgres:latest
       ports:
        - 5433:5432
       environment:
         POSTGRES_PASSWORD: mypassword